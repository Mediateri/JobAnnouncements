<h1>Users#index</h1>
<p class="message"><%= notice %></p>

<% @users.each do |user| %>

   <p>Name : <%= user.name %></p>
   <p>email : <%= user.email %></p>
   <p>Name : <%= user.proffession %></p>
   <p>email : <%= user.email %></p>
   <p> <%= link_to 'details', user_path(user.id) %>
    <%= link_to link_to "edit", edit_user_path(user.id) %>
    <%= link_to link_to 'Delete ', user_path(user.id), method: :delete %></p>
    <br/>
<br/>
<% end %>
<%= link_to 'Post a new user', new_user_path %>

